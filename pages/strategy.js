export default function Strategy() {
  return (
    <div style={{
      backgroundColor: '#000',
      color: '#FFD700',
      minHeight: '100vh',
      padding: '40px',
      fontFamily: 'Arial, sans-serif'
    }}>
      <h1>Strategy Overview</h1>
      <p>We use AI-enhanced order flow models to execute high-frequency trades with precision and speed.</p>
    </div>
  );
}
